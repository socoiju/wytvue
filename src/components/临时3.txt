当你在循环中修改item对象时，你可能会间接地修改k1数组，因为item对象是对k1数组元素的引用。这可能导致children属性出现套娃。
当处理“旅費交通費”数据时，你在k2数组中推送的是item对象的副本，但是item.children可能已经被修改。这可能导致children属性出现套娃。

我在函数中修改item对象导致间接地修改k1数组，是哪一行？复制给我单独这一行。
你在k2数组中推送的是item对象的副本，但是item.children可能已经被修改。这是哪一行？复制给我单独这一行。